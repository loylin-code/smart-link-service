# MCP Servers Configuration
# Loaded on startup alongside database MCP servers

mcp_servers:
  # Example SSE transport MCP server
  # - name: "weather-api"
  #   type: "sse"
  #   endpoint: "https://api.weather.com/mcp"
  #   headers:
  #     Authorization: "Bearer ${WEATHER_API_KEY}"
  #   timeout: 30

  # Example HTTP transport MCP server
  # - name: "calendar-service"
  #   type: "http"
  #   endpoint: "https://calendar.example.com/mcp"
